name: SVG Editor CI

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v3

      - run: echo "🎉 The job was automatically triggered by a ${{ github.event_name }} event."

      - name: Use Node.js
        uses: actions/setup-node@v3
        with:
          node-version: '20'
        
      - name: Go to project directory
        run: cd svg-complex-editor

      - name: Install dependencies
        run: npm install

      - name: Build
        run: npm run build

      - name: Run linting
        run: npm run lint